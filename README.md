# leaders-chronology

A chronology of heads of state and government, built with Deno and Lume.

```sh
deno task serve
deno task build
```

Set `SITE_URL` when building for production to emit absolute Open Graph and `hreflang` URLs.
